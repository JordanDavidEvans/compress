{
  "name": "compress",
  "version": "1.0.0",
  "description": "Static WebP compression site packaged for simple zip-and-serve hosting.",
  "private": true,
  "license": "MIT",
  "scripts": {
    "clean": "rm -rf dist",
    "build": "npm run clean && mkdir -p dist && cp -R src/. dist"
  }
}
